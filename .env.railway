# Railway Environment Variables Template
# Copy these to your Railway project environment variables

# Server Configuration
NODE_ENV=production
PORT=3000

# Database (Railway will create a persistent volume)
DB_PATH=/app/data/rpg.sqlite

# File Uploads (Railway persistent volume)
UPLOAD_DIR=/app/data/uploads

# CORS - Update with your Railway domain
CORS_ORIGIN=https://your-app-name.railway.app

# Authentication - IMPORTANT: Change these for production!
SESSION_SECRET=change-this-to-a-long-random-string-in-production
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your-secure-password-here

# Railway-specific (auto-populated)
RAILWAY_STATIC_URL=${{ RAILWAY_STATIC_URL }}
